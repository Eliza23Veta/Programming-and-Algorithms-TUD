()
**
-x/+x
*,/,%,
+/-
<, <=, >=, 1=, =
NOT x
AND
OR
------------------------------------------------------------------------------------
0,'', []. Empty operators are equivalent to False. Everything else is equivalent to True.

